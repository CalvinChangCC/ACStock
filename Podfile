# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

inhibit_all_warnings!

target 'ACStock' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Swinject'
  pod 'SwiftLint'

  target 'ACStockTests' do
    inherit! :search_paths

    pod 'Quick'
    pod 'Nimble'
    pod 'Fakery'
  end

end
